export interface CardInfo {
    id?: number,
    category?: string,
    image_src?: string,
    transmitter_thumbnail?: string,
    transmitter_name?: string,
    transmitter_id?: number,
    date?: string,
    transmitter_location:string,
    title?: string,
    description?: string 
}